class End:
	def run(self, vm):
		if vm.block.cont():
			vm.jump(block)

class Else:
	def run(self, vm):
		vm.skip(End)

class VM:
	def skip(self, token):
		for i in xrange(self.row, len(self.rows)):
			row = self.rows[i]
			if row and isinstance(row[0], Token):
				 

